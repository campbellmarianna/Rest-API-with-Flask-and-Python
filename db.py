from flask_sqlalchemy import SQLAlchemy # O

db = SQLAlchemy() # It is going to link to our flask app and it is going to look at
# the objects that we tell it to then it is going to allow us to map those objects
# to rows in a database
